# 消息卡片视觉收口 Implementation Plan

**Goal:** 落实已确认的五项视觉审计建议，不改变业务字段、统计口径及渠道模板。
**Architecture:** 复用共享正文和现有 Arco Modal/Image.Preview；只修改推送预览及相关说明。
**Tech Stack:** React、TypeScript、Arco、Less、Node test。
**Spec:** 本轮用户批准的五项视觉审计意见；不进行手机和真实群聊客户端验收。

## Global Constraints

- 保留三类计划、统计指标、异常列表及前往门户文案。
- 不新增公司名称、重复总数或说明性正文。
- 仅修改 412 原型，不提交 Git，不改生产实现。

## Tasks

- [x] 先为监控图片提供具名放大按钮的渲染契约增加测试，在六个渠道/类型组合确认旧实现失败。
- [x] MonitorSnapshot 复用原图片，用 button 打开放大预览，Arco Image.Preview 支持缩放及关闭；类型切换重置预览。
- [x] 共享异常正文改为 13px 深灰/22px 行高；名称 14px；保持普通列表标题大小及企微普通文本样式。
- [x] 收紧统计块与组间距；异常计划三个统计块保持紧凑宽度并居中；企微异常分类独立成行。
- [x] 弹窗 alignCenter=false，顶部固定48px；仅消息区域按最大可用高度滚动，去掉548px最小高度。
- [x] 同步 PRD 图片放大交互。运行 pushStrategyConsistency 测试、build、diff-check，内嵌浏览器核查五个代表状态及图片打开/关闭。

## 验证结果

- 63 项测试通过；新增的图片按钮契约在实现前于六个组合失败、实现后通过。
- TypeScript/Vite 构建通过；保留工程已有的 VChart 循环分块和包体积警告。
- git diff --check 通过。
- 内嵌浏览器检查飞书进度/异常、企微异常、钉钉实时异常和飞书监控图；确认顶部保持稳定、短消息无大块多余留白、完整图片可打开、缩放、关闭并回到消息。
- 按用户要求，不进行手机和真实群聊客户端验收。未提交代码。
