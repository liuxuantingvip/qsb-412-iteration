# 监控筛选与合并明细实施计划

用户已确认：同报表＋同数据日期范围内，只合并值相同的公共字段；店铺、计划、状态及操作保持逐行。新增两条意见仅删除列设置的提示文案，不改变约束。

- [x] 状态组件增加面性 info 入口与“成功 无数据”说明，保留状态与原始字段。
- [x] 日期状态视觉居中；汇总视图支持计划搜索、店铺视图日期筛选，刷新放到状态右侧。
- [x] 下钻平台表头筛选在分页前执行；合并在分页后计算，不跨页、不合并不同值。
- [x] 用同平台、同表的多店铺 mock 展示合并，不把其他平台报表混入同表。
- [x] 同步标注与预览、PRD；测试合并边界、空日期、关键词、状态展示和文字移除，执行构建与差异检查。

仅修改 412 原型。浏览器安全策略限制仍生效，不绕过，不宣称完成真页验证。

验证：36 项相关测试通过，17 条标注 ID 校验通过，TypeScript 与 Vite 构建通过，git diff --check 通过。构建仍有既有 VChart 循环分包和大包提示。真页视觉、浮层 hover、拖拽及表头筛选交互尚未复查。
