# 推送策略 PRD 与交互标注重构

## 已确认范围

保留现有 PRD 章节和已确认业务规则，压缩重复说明。场景表按配置、统计、触达、消息、监控分组；新增四张可渲染流程图。标注只解释具体控件的隐藏规则，使用现有标注侧栏与定位高亮，不改变业务操作。

## 实施与验收

- [x] 重写 PRD：保留最终交付、今日范围、三类计划次数、店铺聚合、实时幂等、有效授权、完整监控图和三渠道差异；未确认分支单列，不编造。
- [x] 补充策略、消息预览和历史的 14 条标注，映射 PRD 行及对应 UI 锚点。
- [x] 接入标注入口与定位事件：切换页签、打开抽屉/预览、切换监控场景；定位不清空未保存的策略内容。
- [x] 配置、定时汇总、实时提醒、监控图片四张 Mermaid 流程图，涵盖取消、校验不通过、无异常及生成失败。
- [x] 检查标注 ID、锚点、事件及 PRD 映射；执行消息回归、文档测试、构建；内嵌浏览器验证流程图和定位。

## 边界

本次不改发送服务、统计实现或渠道配置逻辑，不提交代码。原型模拟值不作为数据来源；不把群消息生成成功等同于客户端送达。保留门户内告警中心和群内处理不在本期的边界。

## 验收结果

- 文档与消息回归：114 项通过。
- 两次构建通过；保留既有 VChart 循环依赖与包体积提示。
- 标注数据与页面锚点扫描各 14 个唯一 ID；实时模式的时间标注落在方式字段，未改动模式或草稿。
- 内嵌浏览器：四张流程图均渲染为 SVG，缩放可用；14 项定位覆盖列表、抽屉、预览、监控和历史，草稿内容保留；验收页无控制台错误。
- 未提交代码。真实发送、生产聚合和深链接入不在本次验收范围。
