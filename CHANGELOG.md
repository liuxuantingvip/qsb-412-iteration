# 版本说明

## v1.0.0（2026-09-04）

取数宝 412 迭代首个完整版本，集中交付 6 项需求的可运行 PRD、交互原型、交互标注、Mock 数据和自动化测试。

### 1. 概览

- 新增账号与服务信息、公告与资源中心、运行指标、机器人排期、运行数据趋势、数据完成与异常等核心板块。
- 支持昨日、周、月周期切换，联动指标、同比、排期与趋势数据。
- 支持数据交付状态下钻、异常分类、运行记录跳转和运行异常重试反馈。
- 补齐 10 项概览交互标注及对应组件预览。

### 2. 数据监控优化

- 支持全部店铺、全部数据表、重点店铺和自定义监控视图。
- 完善日期、平台、关联计划、状态等筛选，增加明细列设置、合并展示和操作入口。
- 统一采集、入库、校验三段结果的状态聚合与失败原因展示。
- 重试后父表、子表共用同一“当前有效执行版本”，保留历史尝试且防止旧响应覆盖新结果。
- 补齐下钻、列设置、状态说明、筛选和重试的交互标注与验收样例。

### 3. 运行详情新增入库日志

- 按单次运行记录展示计划、店铺、连接器和数据表层级。
- 补齐取数日志与入库日志入口，支持执行结果、入库结果筛选及日志长文本查看。
- 入库日志展示数据表中英文名、数据库类型、业务日期、最新有效入库结果及诊断信息。
- 保留尝试历史用于模型校验，页面聚焦最新表结果，不增加入库重试、导出、编辑或删除。

### 4. 门户操作日志

- 为租户管理员提供门户、API、MCP 三类操作日志，普通成员不展示入口。
- 支持时间、操作人、功能模块、操作类型和凭证名称筛选，默认查近 7 天，单次最长 90 天，日志保留 180 天。
- 列表保留操作人和凭证快照，便于凭证撤销或重名后继续追溯。
- 导出范围与当前 Tab 及筛选一致，固定安全字段并对密钥、凭证和表格公式载荷做脱敏处理。
- 只记录成员主动发起且到达服务端的业务操作，不记录浏览、搜索、筛选或系统自动行为。

### 5. 推送策略中心优化

- 完整展示推送渠道配置、推送策略配置和推送历史。
- 支持飞书、企业微信、钉钉群机器人，按渠道原生能力展示卡片或 Markdown 消息预览。
- 定时汇总覆盖计划、店铺、数据表和监控视图；实时推送覆盖异常提醒与成功提醒。
- 统一以“最终交付”判定成功，支持不同对象范围、具体推送时点、前一业务日口径和未保存策略预览。
- 异常提醒按对象首次异常去重，恢复后再次异常可重新提醒；回执成功即记成功，失败只记录不自动重发。
- 推送历史保留实际消息快照、各策略/渠道独立回执和“前往门户”定位入口。

### 6. 公告推送

- 在消息中心展示公告列表、未读状态和公告正文。
- 概览公告可直接打开消息中心并定位对应消息。
- 补齐加载、空数据、失败、重试和已读反馈。

### 共通交付

- 支持 6 项需求的顶部切换和 `PRD / 交互原型` 双视图。
- 交互标注支持按页面定位真实控件，并使用对应前端组件展示验收示例。
- 补齐需求设计、实施计划、评审记录和视觉验收记录。
- 技术栈为 React、TypeScript、Vite、Arco Design、Semi Design 及 VChart。
- 自动化验证覆盖需求注册、状态模型、重试一致性、权限与导出安全、推送协议与规则、标注定位及页面文档一致性。

### 验证结果

- `npm run test:etl`：328 项测试全部通过。
- `npm run build`：TypeScript 检查和 Vite 生产构建通过。
- 构建存在 VChart/Rollup 循环分包与大 chunk 警告，本版本未将其作为功能阻塞项。

### 交付边界

- 本版本是 412 需求 PRD 与交互原型基线，页面数据与服务默认为 Mock。
- 本版本通过只代表原型源码、规则和测试一致，不代表生产前端、后端接口或渠道真实消息已发布。
- CRM 自动化开通已迁移到 413 独立工程，不属于 412 `v1.0.0` 发布范围。
