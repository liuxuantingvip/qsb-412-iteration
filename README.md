# 取数宝 412 迭代

412 迭代 PRD 与交互原型，当前版本为 `v1.0.0`，包含概览、数据监控优化、运行详情新增入库日志、门户操作日志、推送策略中心优化、公告推送六个需求。本地端口为 `5178`。

CRM 自动化开通已迁至相邻的 `413 迭代需求文件/`，独立服务端口为 `5179`。

## v1.0.0 版本内容

完整版本说明见 [CHANGELOG.md](./CHANGELOG.md)。

本仓库交付的是可运行 PRD、交互原型、交互标注、Mock 数据及自动化测试，不代表生产前后端已上线。

## 本地运行

```bash
npm install
npm run dev
```

## 构建验证

```bash
npm run test:etl
npm run build
```
